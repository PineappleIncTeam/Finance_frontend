export interface ISavingsInputForm {
	number?: string;
	date?: string;
	sum?: string;
}

export interface ISavingsSelectForm {
	savings?: string;
}

export interface ITargetAdd {
	name: string;
	amount: string;
}
